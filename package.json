{
  "name": "kenshin-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for @kenshin/ui component library",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@kenshin/*' build",
    "lint": "bun run --filter '*' lint",
    "test": "bun run --filter '*' test",
    "dev": "bun run --filter '*' dev"
  },
  "devDependencies": {
    "shadcn": "^3.7.0"
  },
  "keywords": [],
  "author": "Mauro Cicerchia",
  "license": "MIT"
}
