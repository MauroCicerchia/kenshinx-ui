import { Meta } from "@storybook/blocks";

<Meta title="Getting Started/Introduction" />

# @kenshinx/ui

`@kenshinx/ui` is a personal React UI library built with Tailwind CSS, Radix primitives, and shadcn/ui patterns.

## What You Get

- Accessible building blocks with typed React APIs
- Tailwind-first styling through a shared preset
- Theme tokens powered by CSS variables
- Reusable primitives for application and product work

## Install

```bash
npm install @kenshinx/ui react react-dom tailwindcss
```

## Quick Start

### 1. Add the Tailwind preset

```ts
import type { Config } from "tailwindcss";
import kenshinPreset from "@kenshinx/ui/tailwind-preset";

export default {
  presets: [kenshinPreset],
  content: [
    "./src/**/*.{js,ts,jsx,tsx}",
    "./node_modules/@kenshinx/ui/dist/**/*.js",
  ],
} satisfies Config;
```

### 2. Import the tokens

```ts
import "@kenshinx/ui/styles.css";
```

### 3. Use a component

```tsx
import {
  Button,
  Card,
  CardContent,
  CardHeader,
  CardTitle,
  Input,
} from "@kenshinx/ui";

function App() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Hello World</CardTitle>
      </CardHeader>
      <CardContent className="space-y-3">
        <Input placeholder="Type something..." />
        <Button>Click me</Button>
      </CardContent>
    </Card>
  );
}
```

## Documentation Structure

- `Getting Started`: onboarding and setup
- `Foundations`: theming and shared design primitives
- `Components`: the component catalog, props, and examples

## Next Step

Read the [Theming](/docs/foundations-theming--docs) guide before customizing colors, radius, or dark mode.
